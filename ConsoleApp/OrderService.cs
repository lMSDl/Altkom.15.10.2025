

// Service should validate input, check stock availability, 
// save the order in repository and return Result<OrderResponse>.
// Use async/await and proper exception handling.

/*public interface IOrderService
{
  Task<Result<OrderResponse>> ExecuteAsync(OrderRequest request);
}

public class OrderService : IOrderService
{
  private readonly IOrderRepository _repository;
  private readonly IStockService _stock;

  public OrderService(IOrderRepository repository, IStockService stock)
  {
    _repository = repository;
    _stock = stock;
  }

  public async Task<Result<OrderResponse>> ExecuteAsync(OrderRequest request)
  {
    // Copilot: implement the workflow described above
  }
}*/
